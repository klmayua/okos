import React from 'react';

export default function InitiativeLayout({ children }: { children: React.ReactNode }) {
  return (
    <div className="initiative-layout">
      {children}
    </div>
  );
}
