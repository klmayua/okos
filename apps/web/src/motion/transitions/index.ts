/**
 * OK.OS — MOTION TRANSITIONS
 */

export const transitions = {
  fade: { duration: 300, easing: 'ease-in-out' },
  slideUp: { duration: 400, easing: 'cubic-bezier(0.22, 1, 0.36, 1)' },
  scale: { duration: 300, easing: 'ease-out' },
} as const;
