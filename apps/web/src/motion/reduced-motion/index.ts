/**
 * OK.OS — REDUCED MOTION
 * Disables parallax, blob motion, reduces transition distance.
 */

export const reducedMotion = {
  disableParallax: true,
  disableBlobMotion: true,
  reduceTransitionDistance: true,
  preserveAccessibility: true,
} as const;
